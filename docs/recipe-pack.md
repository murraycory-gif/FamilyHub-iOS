# HUB recipe pack

The catalog schema is [recipe-catalog-schema.md](recipe-catalog-schema.md). Publish that JSON as `recipe-pack.json` on our R2 bucket. The app reads it through `RecipePack` and `RecipePackItem`.
